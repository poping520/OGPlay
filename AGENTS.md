# OGPlay 开发约定

## 开始工作前

1. 使用 UTF-8 读取代码和文本。
2. 先读 `docs/state/CURRENT.md`，按当前改动定位相关模块契约及在办任务；只在涉及跨模块
   接口时读相邻契约，追溯时才读已完成验收。同一会话已读且未变化的内容不重复读取。
3. 在 `capabilities.toml` 中确认能力现状，不凭猜测补实现。
4. 适配新游戏、跑测试或排查症状前，先读 `docs/playbook/` 下对应的操作手册
   （总览 `docs/playbook/README.md`），按既有流程做，不要另起一套。

## 范围边界

OGPlay 是安卓老游戏兼容层，不是 Android 模拟器。只实现游戏进程会直接调用的能力。
禁止引入完整 Android 系统、Binder/system_server/Zygote、完整 ART/Dalvik、Play 服务、
现代支付/社交/反作弊或手机端运行能力。范围有争议时先写 ADR。

## 架构规则

- 依赖方向只能由上层指向下层；回调必须通过显式接口。
- `src/` 中禁止出现游戏名、厂商名、包名和游戏专属分支；差异只能进入 `data/profiles/`。
- 每个生产代码目录必须有 `MODULE.md`；契约与代码冲突时以契约为准。
- guest 指针必须使用强类型包装。
- 未实现能力必须记账、可查询并明确失败，禁止伪造成功和静默返回零。
- 所有时间源必须通过统一 Clock；线程模型为一个 guest 线程对应一个宿主线程。
- 图形使用 ANGLE，窗口与输入使用 SDL3；禁止重新引入手写 GLES 到桌面 GL 转译。
- 禁止在 `src/` 裸用 `printf`、`std::cout`、`std::cerr`；使用结构化日志。

## 按改动选择最小工作流

目标是以最少必要工作交付可验证结果，不以文件数、测试数或文档量证明完成。
Agent 自行按实际风险分级，无需为常规分级向用户确认；出现新依赖或架构变化时再升级。
本节统一约束操作手册中的收尾要求，不把手册列出的文档当作每次必改清单。

| 类型 | 默认工作范围 |
| --- | --- |
| 配方/数据扩充 | 固定来源原版类或既有数据加入现有机制：修改配方及必要生成产物，复用现有校验；不默认新增 C++ 测试、任务单或修改 MODULE |
| 局部行为修复 | 最小实现及直接相关回归；已有测试能证明修复时复用，不为每个方法另建任务 |
| 新能力/架构变化 | 明确支持范围，建立任务单；按真实契约变化更新 MODULE，架构决定追加 ADR |
| 纯文档 | 仅 UTF-8、链接及差异静态检查，不构建、不运行代码测试 |

- 小改动直接执行，不先写长计划。不得顺带重构、整理历史或修复下一独立问题；用户要求
  连续推进或下一问题是本次验收的必要依赖时除外。
- 工作单按通用能力组织，不按每条报错创建。简单配方和局部修复可记入现有任务或当前状态；
  复杂工作显式列依赖与验收边界，一个 Work Unit 应能在单次会话完成。

## 验证与停止条件

- 每个行为变更需要机器可判定的验证证据，不等于每次必须新增测试代码。优先复用现有
  定向测试、制品检查或有明确失败判据的复现；新增逻辑或现有检查覆盖不到的风险才补测试。
  quirk 仍必须有“关闭即失败”的测试。未修改的原版算法不自动要求新增完整语义测试。
- 只构建受影响目标，Windows 使用 `windows-msvc`。只有配方/运行时数据变化且现有二进制
  可直接加载时，不重编译 C++。无用户明确要求禁止全量测试；不运行与改动无关的门禁。
- BootDex 配方扩充：核对依赖/native/overlay 冲突，build/check，并按需要复用已有链接或
  加载检查。出现新 native、未满足依赖或行为适配才升级为代码开发；不为类数变化重写测试。
  若已有硬编码检查确需更新，只做必要维护，不在小任务中重构测试框架。
- 修复运行首错时，优先复现相同触发路径一次，记录原错消失及下一首错；这是 reached-fault
  证据，不等于游戏兼容验收。只有正式 title gate/golden 验收才要求 Scenario 三轮复跑。
- 相关检查通过且无新疑点后停止，不重复 build/check 或扩大测试矩阵。无法验证时明确缺口，
  不用无关测试替代。长任务可记录各阶段耗时，便于区分实现、构建和复现成本。

## 文档按事实归属更新

- `MODULE.md` 只记录职责、接口、不变量及支持边界；这些发生变化才修改。不要将新增类名、
  类数、测试计数或修复历史同步抄入多个 MODULE。未改契约的模块不更新。
- 类集合以配方为准，制品哈希/大小/类数以生成 manifest 为准，文档不重复维护这些数值。
- `capabilities.toml` 仅在能力范围、状态或验证入口变化时更新，优先维护现有条目；配方增加
  几个原版类不默认新建能力项。核对不等于必须修改，未验证能力不能标为完成。
- `CURRENT.md` 只在运行状态、当前阻塞或重要里程碑变化时做最小更新，不顺手压缩其他历史；
  纯工作流文档修改无需更新运行快照。简单修复的一条记录可同时交代改动、验证和下一首错。
- 最终简洁说明改动、实际验证及剩余缺口。禁止因“同步文档”批量触碰无关文件。
