# 状态只能按 unimplemented -> stub -> partial -> complete 前进。

[core.structured_logger]
status = "partial"
test = "tests/core/logger_tests.cpp"
note = "M0 已有线程安全环形缓冲；sink、限流、JSONL 与符号化待后续 WU"

[core.capability_ledger]
status = "partial"
test = "tests/core/capability_ledger_tests.cpp"
note = "M0 支持账本读取和未实现命中计数；CI 单调性门禁待补"

[agent.control_service]
status = "partial"
test = "tests/agent/control_service_tests.cpp"
note = "M0 提供进程内结构化分派；JSON-RPC 传输和参数模型待补"

[agent.session]
status = "stub"
test = "tests/agent/control_service_tests.cpp"
note = "只返回 idle 状态"

[agent.run]
status = "stub"
test = "tests/agent/control_service_tests.cpp"
note = "无会话时明确失败，M1 接入统一 Clock"

[agent.sym]
status = "stub"
test = "tests/agent/control_service_tests.cpp"
note = "无 guest 映像时明确失败，M2 接入 ELF 符号表"

[golden.software_frame]
status = "partial"
test = "tests/golden/golden_frame_tests.cpp"
note = "M0 验证无 GPU 图像差异算法；ANGLE 软件后端属于 M4"

[runtime.syscall]
status = "unimplemented"
test = ""
note = "M2"

[runtime.jni]
status = "unimplemented"
test = ""
note = "M3"

[gles.angle]
status = "unimplemented"
test = ""
note = "M4"

