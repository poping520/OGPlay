# WU-0002 · C++20/CMake 构建入口

目标：提供不依赖游戏文件的三平台 C++20 配置、编译、测试和安装入口。

依赖：WU-0001。

文件：`CMakeLists.txt`、`CMakePresets.json`、`cmake/*.cmake`、`README.md`。

验收：

- [x] Windows/MSVC 配置成功；通用 dev/ci 预设已生成并纳入三平台 CI。
- [x] Debug 与 Release 均生成 `ogplay`、`ogplay_core`、`ogplay_agent` 和测试目标。
- [x] CTest 全绿（13/13）；Release 在 warnings-as-errors 下通过。
- [x] MSVC 与 GCC/Clang 警告策略统一，release/CI 将警告视为错误。
- [x] `src/` 裸输出有独立架构门禁。
