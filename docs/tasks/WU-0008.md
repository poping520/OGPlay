# WU-0008 · 初始化本地 Git 仓库

目标：为正式版建立本地版本库，同时确保仅作参照的 DEMO 不进入索引。

依赖：WU-0001。

文件：`.gitignore`、`.gitattributes`、`README.md`、`docs/state/CURRENT.md`、本任务单。

验收：

- [x] 规划文档链接统一指向 `docs/roadmap/`。
- [x] `.gitignore` 明确排除整个 `docs/demo/`。
- [x] `git check-ignore docs/demo/` 确认规则生效。
- [x] 初始提交索引中不存在 `docs/demo/` 下的文件。
