# WU-0011 · 能力账本单调性门禁

目标：在本地和三平台 CI 中拒绝能力条目删除或状态后退。

依赖：WU-0004、Git 仓库。

验收：

- [x] 状态顺序固定为 unimplemented < stub < partial < complete。
- [x] 当前工作树默认与 HEAD 比较，CI 使用 fetch-depth 2 与 HEAD^ 比较。
- [x] 删除已有能力或降低状态会令 CTest 架构门禁失败。
- [x] 新增能力不受阻，运行时未实现命中仍由 CapabilityLedger 聚合。
