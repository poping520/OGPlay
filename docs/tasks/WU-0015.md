# WU-0015 · M1 第三方 submodule 基线

目标：以固定 Git submodule 引入 M1 所需 SDL3、Dynarmic，并迁入既有 doctest。

依赖：WU-0014、ADR-0003、ADR-0005。

验收：

- [x] doctest 固定为 2.4.11，SDL 固定为 3.4.10，Dynarmic 固定为 DEMO 验证提交。
- [x] Dynarmic 递归依赖可由 `git submodule update --init --recursive` 完整初始化。
- [x] CI checkout 使用 recursive submodules。
- [x] 来源、版本与许可证已记录，`docs/demo/` 未进入版本控制。
