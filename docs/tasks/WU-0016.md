# WU-0016 · 离线第三方构建入口

目标：让默认 CMake 只使用仓库固定的 submodule，并在依赖未初始化时明确失败。

依赖：WU-0015、ADR-0007。

验收：

- [x] 默认配置不含 FetchContent 或网络下载路径。
- [x] SDL3 默认进入配置；Dynarmic 在 CPU Work Unit 启用前保持显式关闭。
- [x] 缺失 submodule 时 CMake 输出递归初始化命令并失败。
- [x] MSVC warnings-as-errors 全量测试通过；GCC 在本会话总体验收统一执行。
