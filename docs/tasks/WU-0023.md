# WU-0023 · Checked MemoryBus soft-MMU

目标：为解释器和调试器提供确定性小端访存与可观察的 soft-MMU 后端。

依赖：WU-0022、CPU 模块契约。

验收：

- [x] MemoryBus 固定 8/16/32/64 位小端读写，不暴露宿主指针。
- [x] 跨页写在复制前验证完整范围，失败不产生部分写入。
- [x] 成功访问向显式 observer 报告地址、宽度、类型和 guest 线程 ID。
- [x] MemoryFault 原样传播，不伪造读取值。
- [x] MSVC warnings-as-errors 全量 CTest 通过（46/46）。
