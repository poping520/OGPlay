# WU-0024 · 最小内存快照骨架

目标：让 guest 内存映射、权限和内容能够确定性保存并事务式恢复。

依赖：WU-0022、memory 模块契约。

验收：

- [x] 快照包含显式版本、宿主页尺寸、映射区间、权限和字节内容。
- [x] 连续相同权限映射合并保存，恢复后字节和权限一致。
- [x] 恢复先构造完整替代地址空间，成功后再替换当前状态。
- [x] 无效版本、页尺寸、区间或内容长度明确失败且不改变当前状态。
- [x] MSVC warnings-as-errors 全量 CTest 通过（49/49）。
