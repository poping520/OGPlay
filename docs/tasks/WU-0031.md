# WU-0031 · Dynarmic 轻量 Boost 依赖

目标：以固定 Git submodule 提供 Dynarmic 构建所需的裁剪 Boost 头文件。

依赖：WU-0015、cpu 模块契约。

验收：

- [x] `ext-boost` 以独立 submodule 固定到明确提交，不引入完整 Boost 超级仓库。
- [x] Dynarmic 配置只从仓库内的 `ext-boost` 查找 Boost 头文件。
- [x] 来源、版本、许可证和用途已记录。
- [x] Windows/MSVC 可在启用 Dynarmic 时完成 CMake 配置。
