# WU-0033 · 宿主真线程 HAL

目标：提供不泄漏平台实现细节、可显式 join 的真实宿主线程边界。

依赖：WU-0018、hal 模块契约。

验收：

- [x] 非空 entry 启动真实宿主线程，线程 ID 可用于验证一对一映射。
- [x] join 生命周期明确；空 entry 和重复 join 明确失败。
- [x] 多个线程可同时到达同步点，未退化为协作式串行调度。
- [x] Windows/MSVC warnings-as-errors 全量测试通过（67/67）。
