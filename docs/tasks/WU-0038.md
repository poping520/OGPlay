# WU-0038 · CPU 停止结果跨编译器初始化

目标：消除 M1 三平台严格告警发现的 CPU 聚合尾字段隐式初始化。

依赖：WU-0031、WU-0032、cpu 模块契约。

验收：

- [x] 解释器与 Dynarmic 的所有停止路径显式初始化完整 `RunResult`。
- [x] Dynarmic 回调的所有待处理停止显式初始化完整 `PendingStop`。
- [x] guest 线程测试替身遵守相同结果契约。
- [ ] Windows/Linux/macOS warnings-as-errors 构建与全量测试通过。
