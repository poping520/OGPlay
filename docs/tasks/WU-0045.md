# WU-0045 · ELF 字段跨编译器类型安全

目标：消除三平台严格编译发现的 ELF16 位字段隐式窄化。

依赖：WU-0043、loader 模块契约。

验收：

- [x] ELF16 位字段在 32 位无符号类型中移位和合并，最后显式收窄。
- [x] 既有正常与畸形 ELF 测试覆盖结果不变。
- [x] Windows/Linux/macOS warnings-as-errors 构建与全量 CTest 通过（80/80）。
