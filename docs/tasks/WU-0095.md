# WU-0095 · API 23 VMA 元数据

目标：实现 API 23 Bionic allocator 的 `PR_SET_VMA_ANON_NAME`，不丢弃内核元数据。

依赖：WU-0094、WU-0085 的 API 23 syscall 轨迹。

验收：

- [x] 仅接受 `PR_SET_VMA/PR_SET_VMA_ANON_NAME` 与非零范围。
- [x] 完整范围按映射存在性校验，`PROT_NONE` guard page 可命名。
- [x] 名称最多 79 字节并要求 NUL；显式 sink/无头报告保留 annotation。
- [x] Windows/MSVC warnings-as-errors 与 API 23 累计样本通过且无未实现命中。
