# WU-0100 · ELF 链接模块聚合初始化

目标：让新增的 `load_ranges` 事实在 GCC/Clang/MSVC 严格告警下均被显式初始化。

依赖：WU-0096、三平台 warnings-as-errors 出口。

验收：

- [x] 总装载器构造链接模块时显式初始化空 load range，装载后再写入实际 `PT_LOAD` 页范围。
- [x] 不改变模块顺序、符号解析或事务回滚行为。
- [ ] Linux/macOS warnings-as-errors 增量构建与全量 CTest 通过。
