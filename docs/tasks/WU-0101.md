# WU-0101 · VFS 迭代器偏移类型

目标：让 64 位 VFS offset 到容器迭代器的转换在 GCC/Clang/MSVC 严格告警下显式且安全。

依赖：WU-0083、三平台 warnings-as-errors 出口。

验收：

- [x] Read/Write 在既有范围与文件尺寸检查后显式转换为 vector difference type。
- [x] 不改变大文件 EFBIG、EOF、descriptor offset 或读写内容语义。
- [ ] Linux/macOS warnings-as-errors 增量构建与全量 CTest 通过。
