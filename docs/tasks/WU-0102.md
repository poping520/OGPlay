# WU-0102 · 无头请求完整聚合初始化

目标：让 M2 累计测试在 GCC/Clang/MSVC 下显式初始化 tick 预算与可选 syscall observer。

依赖：WU-0085、三平台 warnings-as-errors 出口。

验收：

- [x] 测试请求显式使用生产默认预算并把 observer 初始化为空。
- [x] 不改变 API 19/22/23 的 guest 入口、VFS 输出或可观测性断言。
- [x] Linux/macOS warnings-as-errors 增量构建与全量 CTest 通过。
