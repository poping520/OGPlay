# DVM-187 · View 焦点与 WebView 配置边界

## 范围

1. View 焦点、监听器、scroll-container 与滚动条开关使用逐实例状态和 UiTree 单一焦点 owner。
2. 补齐当前 APK 使用的 WebSettings 配置、RenderPriority、client 默认回调及 GC 引用。
3. 页面加载、JavaScript、历史和导航不引入浏览器后端；后续禁用网页策略见 DVM-188。

## 验收

- Windows Release 定向 3 用例/588 断言通过：隐藏视图拒绝焦点、焦点转移/回调、实例隔离、
  配置默认值与读写、client/JavaScript interface GC 保活及 destroy 生命周期。
- UiTree 回归 15 用例/59 断言通过。
- 按用户安排未复跑游戏；真实初始化链结果待用户测试。

状态：实现完成，等待真实 APK reached-fault。
