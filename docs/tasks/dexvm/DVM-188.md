# DVM-188 · 默认禁用网页策略

## 范围

1. WebView HTTP/数据加载通过主 Looper 异步报告 `ERROR_UNSUPPORTED_SCHEME`，不产生成功回调、
   页面层或输入层；stop/destroy 可取消待回调，历史保持为空。
2. `javascript:` 只记录并跳过；外部 HTTP/HTTPS ACTION_VIEW 只记录，不启动宿主浏览器。
3. 日志按 action 去重，保存完整 URL，并额外拆分 host 与去查询参数后的 path；开发配置可恢复严格异常。

## 验收

- Windows Release 构建通过。
- 两个解释器后端覆盖异步失败、loadData、JS 跳过、stop/destroy 取消、空历史、严格模式、
  外部浏览器拦截和显式 Activity 状态不变。
- 按用户安排未复跑游戏；“教程确认 → 网页请求”路径由用户验收。

状态：实现完成，等待真实 APK reached-fault。
