# GUI-4 · 全局设置

状态：Windows 全局设置完成；运行时预留项保持显式标记。依赖：[GUI-3](GUI-3.md)。Linux 暂缓。

- 十组设置由统一字段定义驱动；类型、枚举、数值范围与目录严格校验。
- GuiConfig 写入 schema 2，读取兼容 schema 1，保留 `.bak` 中断恢复；损坏配置不覆盖。
- 保存前比较读取版本，过期编辑明确失败；未保存离开需确认，恢复默认仍须保存。
- 主题、密度、异常日志提示、启动最小化、Profile 目录、导入数据目录预填接入行为。
  超采样、解释器与 MCP 仅转发已有 CLI 参数；其余选项明确标注预留，仅持久化。
- 关于页读取随程序交付的构建/Android 制品清单和第三方许可；缺失明确显示。

验证（2026-09-22）：

- Windows Release `ogplay-gui`、`ogplay_tests` 构建通过。
- `npm run build`：类型检查、14 项前端用例和静态制品生成通过。
- GUI/control_service 57 项定向用例通过，覆盖 schema 1 升级、schema 2 类型/范围、
  `.bak` 恢复、无效保存不替换原配置、旧版本拒绝、后台分析期间拒绝保存、损坏配置保护、
  目录打开边界、已支持/预留参数隔离及仅启动成功后请求最小化。
- 4 项 `frontend.gui_*` CTest 通过。
- 原生 Windows 交互验证：十组导航、浅色主题保存、未保存离开提醒/继续编辑、关于页、
  关闭重启后的主题保留。测试配置位于 `.local/gui-v2/library-04/`，测试窗口已关闭。

证据：`.local/gui-v2/v2-04-{release-build,test-build,webui,tests,ctest}.log`。
未运行全量测试或真实游戏；最小化的调用时序由注入宿主回归验证，未实测启动游戏后窗口最小化。
语言翻译、音频/输入/网络等预留控制、沙盒统计/清理、FFmpeg 探测、诊断包导出、
MCP 端口范围自动分配尚未实现；UI 明确显示仅保存或暂未开放。
