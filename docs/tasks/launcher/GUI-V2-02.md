# GUI-V2-02 · 前端游戏库

状态：实现完成，Windows 交互验收部分通过；原生文件选中和跨窗口拖放待验收。

范围：网格/列表、搜索筛选与排序、安装实例详情抽屉、空态、图标光晕、状态角标，
以及安装包选择/拖放入口。详情使用宿主返回的版本、导入时间和目录事实；损坏元数据
返回 null，不推测版本。选择按 installation id 区分同包多版本。

安装包入口检查单文件、扩展名和非空内容，随后明确提示尚未导入；实际导入属于
GUI-V2-03。设置与 Dashboard 尚未开放。Linux 暂缓。

验证（2026-09-22）：

- Windows Release 受影响目标构建通过。
- `npm run check`：TypeScript 与 9 个前端用例通过。
- GUI/control_service 定向回归：49 用例、354 断言通过。
- 4 项 `frontend.gui_*` CTest 通过，包含空库/CJK 非空库真实 WebView 加载与 RPC。
- 原生窗口自动交互：列表切换、同包多版本搜索、正确实例详情、运行中筛选空态通过。
- 原生安装包选择框可打开；自动化工具无法定位其 WebView2 子窗口，未完成文件选中，
  也未完成跨窗口拖放。文件校验已有单元测试，不能代替上述原生交互验收。

未运行全量测试或真实游戏兼容验收。
