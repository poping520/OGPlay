# WU-0001 · 仓库自举与工程规则

目标：让新会话只读仓库文档即可理解范围、架构决定、当前状态和验证流程。

依赖：无。

验收：

- [x] 顶层 `AGENTS.md` 写明范围、依赖、可观测性与交接规则。
- [x] `docs/` 具备 ADR、模块、playbook、state、tasks 索引。
- [x] 已补录 HLE、Bionic、ANGLE/SDL3、线程/时钟、构建测试、可观测性决定。
- [x] `docs/state/CURRENT.md` 能作为下一会话入口。

