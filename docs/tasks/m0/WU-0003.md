# WU-0003 · 正式版模块边界

目标：按规划建立细粒度正式版目录与单向依赖契约，不迁移 DEMO 巨型文件。

依赖：WU-0001。

验收：

- [x] core/hal/cpu/memory/loader/runtime/gles/audio/input/session/agent/frontend 均有契约。
- [x] 契约声明职责、公共 API、不变量、禁止项和测试位置。
- [x] 下层不得反向依赖、游戏特判仅允许进入 profile、源文件上限 800 行。
- [x] 日志、线程、时钟、ANGLE 和显式失败约束已进入对应模块。

