# WU-0006 · 完成 M0 日志与诊断后端

目标：把当前 Logger 骨架扩展为规划要求的 Console/File/JSONL/Ring/Agent 多 sink、限流、
guest 地址自动符号化和崩溃转储基础设施。

依赖：WU-0004、M2 ELF 符号接口可先以抽象 provider 替代。

验收：

- [x] 热路径在无 sink 时只写有界环形缓冲；性能上限在真实 guest 基准建立后持续度量。
- [x] 固定 message/fields、分类 sink、首次计数、每 N 帧与令牌桶契约测试通过。
- [x] `GuestAddress` 渲染调用符号 provider；无符号时保留原始十六进制地址。
- [x] 异常触发可转储 summary.txt、ring.jsonl、unimplemented.json 和 session.json。
