# WU-0010 · 无 GPU 软件黄金帧后端

目标：让 M0 的黄金帧基础设施不仅能比较图像，还能在没有窗口/GPU 时确定性地产生帧。

依赖：WU-0004。

验收：

- [x] SoftwareSurface 可确定性 clear、裁剪矩形并暴露 RGBA8 ImageView。
- [x] 相同命令序列的像素差和感知哈希距离均为 0。
- [x] 单像素变化可被像素差检测，越界矩形安全裁剪。
- [x] 不引入平台窗口或 GPU 依赖；ANGLE/SwiftShader 一致性仍属于 M4。
