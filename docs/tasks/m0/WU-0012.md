# WU-0012 · Null-call 运行时账本

目标：让任何吞掉空调用的兼容行为都能按 LR 与符号聚合并供 Agent 查询。

依赖：WU-0004。

验收：

- [x] 相同 LR+symbol 的调用累计计数，不同符号不混合。
- [x] 快照为值拷贝，调用方不能修改内部账本。
- [x] 账本为空时返回空数组，不伪造成功事件。
- [x] core 不依赖 runtime/quirk 实现。
