# WU-0014 · M0 总体验收与交接

目标：用两套编译器、全量测试、CLI 调试闭环和架构扫描冻结 M0，并给 M1 明确入口。

依赖：WU-0001 至 WU-0013 中所有 M0 工作单。

验收：

- [x] MSVC Release + warnings-as-errors 编译，CTest 27/27。
- [x] Cygwin GCC 14.4 Release + `-Werror` 编译，CTest 27/27。
- [x] stdio 连续执行 open → step → state，frame/ticks 保持一致。
- [x] `src/` 无游戏特判、无裸输出、无超过 800 行源文件。
- [x] `M0-ACCEPTANCE.md` 记录证据、范围边界和 hosted CI 外部门禁。
