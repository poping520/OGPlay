# WU-0017 · M1 统一 Clock 后端

目标：实现可确定性验证的实时/固定步长、快进与暂停统一时钟。

依赖：WU-0007、ADR-0004。

验收：

- [x] 固定步长使用有理数倍率并跨调用保留余数。
- [x] 实时时钟只依赖单调时间源，暂停/恢复/切换倍率不丢 ticks。
- [x] 不支持的实时按帧推进、非法倍率与溢出明确失败。
- [x] Session 暂停/恢复同步控制其唯一 Clock。
- [x] MSVC 与 GCC warnings-as-errors 全量测试通过（30/30）。
