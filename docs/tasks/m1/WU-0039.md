# WU-0039 · CPU 样例遵守 W^X

目标：让 CPU 指令和 M1 样例测试在禁止同时写入和执行的宿主上运行。

依赖：WU-0031、WU-0037、memory 与 cpu 模块契约。

验收：

- [x] 所有 CPU 测试代码页先以 RW 装载，再切换为 RX 执行。
- [x] 执行权限失败用例仍验证只读代码页产生显式 fault。
- [x] Windows/Linux/macOS warnings-as-errors 构建与全量测试通过（73/73）。
