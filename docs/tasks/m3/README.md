# M3 Work Unit

M3 任务从文档准备工作 `WU-0104` 开始，运行时开发从 `WU-0105` 继续。范围为 JNI 类型与函数表、引用、异常、方法调用、线程附着、
最小框架对象和 DEX L1 决策所需的契约。不得提前引入完整 ART/Dalvik 或 M4 图形栈。

`WU-0104..0143` 的功能开发任务已经完成。按新的结构要求，`WU-0144` 起在出口前拆分
runtime 子模块；拆分完成后再创建 Windows、Linux、macOS warnings-as-errors 与累计无界面
契约出口 WU。在三平台证据齐全前，M3 不标记为已验收。
