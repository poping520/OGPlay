# WU-0120 · JNI 对象数组

目标：实现带 Java 类可赋值约束的 JNI 对象数组存储。

依赖：WU-0116。

验收：

- [x] 数组冻结已注册元素类，支持指定长度 null 初始化和兼容初值填充。
- [x] 非 null 元素携带对象身份和声明类，子类赋值被接受，不相关类被拒绝。
- [x] 长度、索引、数组身份、元素类及元素值错误均明确分类失败。
- [x] 对象数组身份可发布为普通 JNI 引用。
- [x] Windows/MSVC warnings-as-errors 构建与全量 CTest 通过（201/201）。
