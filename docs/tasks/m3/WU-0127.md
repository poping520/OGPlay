# WU-0127 · DEX L1 分析报告

目标：从只读 DEX 事实生成可批量汇总的引擎指纹与 Java 厚度报告。

依赖：WU-0126。

验收：

- [x] 输出总类、应用类、声明方法、native 方法和 instruction code-unit 数量。
- [x] 单独统计 onDraw/onDrawFrame override 数量和指令规模。
- [x] Java 厚度由明确阈值分为 none/thin/moderate/thick，并给出 DEX 执行候选布尔值。
- [x] framework/intrinsic 类不进入应用代码规模统计。
- [x] 引擎签名由 library basename 与所需导出符号声明式注入，无生产代码硬编码。
- [x] class_data 对应关系和签名 catalog 冲突均明确失败。
- [x] Windows/MSVC warnings-as-errors 构建与全量 CTest 通过（222/222）。
