# WU-0129 · Android Context 类层级

目标：建立 Activity 所需的最小 Context/ContextWrapper 继承链，并为后续资源 HLE 冻结调用契约。

依赖：WU-0119。

验收：

- [x] 声明 Object→Context→ContextWrapper→Activity 类层级。
- [x] Bundle 保持 Object 的直接子类。
- [x] Activity 可继承查找到 `Context.getAssets()`。
- [x] 资源 HLE 尚未安装时调用 `getAssets()` 明确报告 missing handler。
- [x] Windows/MSVC warnings-as-errors 构建与全量 CTest 通过（222/222）。
