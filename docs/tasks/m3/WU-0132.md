# WU-0132 · Context 偏好服务契约

目标：在 Context 类层级冻结 SharedPreferences 服务入口，让行为实现保持可插拔且缺失时明确失败。

依赖：WU-0129。

验收：

- [x] Context 声明 `getSharedPreferences(String,int)` 精确描述符。
- [x] Activity 通过 ContextWrapper 继承查找到该方法。
- [x] 偏好 HLE 未安装时调用明确报告 missing handler。
- [x] Windows/MSVC warnings-as-errors 构建与全量 CTest 通过（225/225）。
