# WU-0144 · Runtime 子模块契约

目标：冻结 runtime 七个子模块的所有权与无环依赖方向，并为每个子模块建立模块文档。

依赖：ADR-0013、WU-0143。

验收：

- [x] jni、framework、bionic、syscall、execution、vfs、integration 均有独立 MODULE.md。
- [x] 根 runtime 契约提供可跳转子模块索引。
- [x] ADR 明确公共头/实现镜像目录、迁移规则与依赖方向。
- [x] guest_thread_lifecycle 与 runner/clone 的归属消除 syscall/execution 循环。
- [x] 本 WU 只建立契约，不混入代码行为或物理迁移。
