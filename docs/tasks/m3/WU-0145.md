# WU-0145 · Runtime 子模块索引与文档门禁

目标：让七个 runtime 子模块契约可发现、不可缺失，并同步 M3 出口前结构整理状态。

依赖：WU-0144。

验收：

- [x] docs/modules/INDEX.md 提供七个子模块的可跳转契约链接。
- [x] 文档布局测试逐一要求七个 `src/runtime/<submodule>/MODULE.md` 存在。
- [x] CURRENT 恢复为小于 6 KiB 的滚动快照并交接到 VFS 迁移。
- [x] M3 任务索引、目录说明和待出口验收文档不再把 WU-0144 误记为出口任务。
- [x] Windows/MSVC 文档布局门禁通过（1/1）。
