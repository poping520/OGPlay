# WU-0146 · VFS 子模块物理迁移

目标：把 VFS 公共头和实现迁入镜像子目录，并消除仓库内旧 include 路径。

依赖：ADR-0013、WU-0145。

验收：

- [x] 公共头位于 `include/ogplay/runtime/vfs/vfs.h`。
- [x] 实现位于 `src/runtime/vfs/vfs.cpp`，CMake 只编译新路径。
- [x] framework、syscall、integration 与 VFS 测试均引用新公共路径。
- [x] 仓库源码中不再存在 `ogplay/runtime/vfs.h` 引用或旧文件。
- [x] Windows/MSVC warnings-as-errors 构建、VFS 定向测试 13/13 与全量 CTest 232/232 通过。
