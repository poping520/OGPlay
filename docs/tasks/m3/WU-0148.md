# WU-0148 · M3 三平台出口兼容性加固

目标：修复 M3 出口严格构建暴露的 CMake 策略与 DEX 无符号位拼接可移植性问题。

依赖：WU-0147、M3 三平台出口预检。

验收：

- [x] 文档布局脚本显式启用 `IN_LIST` 所需策略，旧版 CMake 不再按 OLD 行为解析。
- [x] DEX 16 位字段和 Modified UTF-8 code unit 均先在 32 位无符号类型中组合，再显式收窄。
- [x] 修改不改变 DEX 解码结果，既有正常与畸形输入测试全部通过。
- [x] Windows/MSVC、Linux/GCC、macOS/AppleClang warnings-as-errors 构建通过。
- [x] 三平台全量 CTest 均通过 232/232。
