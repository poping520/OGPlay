# WU-0192 · 三平台 NativeActivity 测试后端

目标：让 M4 两个真实 NativeActivity APK 测试在各宿主平台选择与 CLI 一致的 ANGLE
硬件后端，解除 Linux/macOS 的 D3D11 硬编码阻塞。

依赖：WU-0191、WU-0152。

验收：

- [x] Windows 测试选择 D3D11/hardware。
- [x] Linux 测试选择 Vulkan/hardware。
- [x] macOS 测试选择 Metal/hardware。
- [x] 最小 APK 与综合出口 APK 共用同一宿主后端选择函数。
- [x] Windows/MSVC + ANGLE、真实 minimal/M4 APK 与 API 19 Bionic 环境全量 CTest
  295/295 通过。

非目标：本 WU 不执行 Linux/macOS 测试，不改变 CLI 或运行时后端逻辑，也不允许硬件测试
静默回退 SwiftShader。
