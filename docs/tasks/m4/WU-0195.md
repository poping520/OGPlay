# WU-0195 · M4 出口候选冻结

目标：在不执行远端测试的前提下完成 Windows 严格出口预演，并把同一代码状态交给
Linux/macOS 对应机器本地验收。

依赖：WU-0194。

验收：

- [x] 本机出口入口以真实 API 19 Bionic、最小 APK 和综合 APK 通过全部前置预检。
- [x] 综合 APK 的按键/指针输入严格回归连续执行 5 次均通过。
- [x] Windows/MSVC D3D11 硬件黄金帧、两个 APK 真实渲染/输入及完整全量 CTest
  297/297 通过。
- [x] Windows 清单未启用 SwiftShader时仍验证明确失败且不回退硬件。
- [x] Linux/macOS 命令仅使用 `tools/m4_exit.py` 在对应机器本地执行，不从 Windows
  通过 SSH 代跑。
- [x] 生成的 Python cache 被全局忽略，不污染出口候选工作树。

非目标：本 WU 不宣称 M4 已完成；Linux Vulkan/hardware、macOS Metal/hardware 及两平台
Vulkan/SwiftShader 黄金帧通过后，才能编写 `M4-ACCEPTANCE.md` 并更新里程碑任务索引。
