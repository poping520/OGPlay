# WU-0224 · First exact legacy title Profile

目标：以纯数据 Profile 描述目标 APK 已核验的身份、ABI 与通用运行时选择。

依赖：WU-0223、`data/profiles/title-profile-v1.schema.json`、目标 APK Manifest/ELF 事实。

验收：

- [x] 文件名与 package 精确一致，versionCode 1、native SHA-256 与 `armeabi` 均来自 APK。
- [x] API 19、`gl_surface_view` 与 800×480 只作为已有通用运行时/surface 数据声明。
- [x] Profile 不含脚本、quirk、标题专属 handler 或 `src/` 分支，且少于 200 行。
- [x] Python schema、C++ 目录、quirk 交叉校验与全量 CTest 358/358 通过。
- [x] 目标 APK `run-apk --preflight` 精确命中并完成 5 模块真实 Bionic 依赖闭包。

非目标：本 WU 不执行 guest、不声明 GLES1/JNI 生命周期已实现，也不迁移演示特判。
