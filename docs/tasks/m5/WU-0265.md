# WU-0265 · GLES1 material face quirk declaration

目标：登记目标的 GLES1 material face 规范差异，并让精确 Profile 与 CLI 在执行前受检启用。

依赖：WU-0204、WU-0205、WU-0264、`src/frontend/MODULE.md`。

验收：

- [x] quirk 注册项完整声明 summary、reason、risk、test 与 owner。
- [x] 精确 Profile 启用 quirk 并提供显式参数表，仍不超过 200 行。
- [x] `run-apk` 强制加载仓库 quirk 注册表后再构造 Profile catalog。
- [x] 未注册、缺参数或测试引用失效在 APK 执行前明确失败。
- [x] quirk 引用的机器测试证明关闭时 `GL_FRONT` material face 明确失败。
- [x] Python/C++ Profile 与 quirk 门禁及 macOS-arm64 + ANGLE 全量 CTest 通过。

非目标：本 WU 不启用运行时归一行为；声明到 guest-session 的映射由 WU-0266 完成。

机器证据：

- `GLES1 material front face quirk is required when disabled`：quirk 未映射时
  `GL_FRONT (0x0404)` 仍明确失败。
- `repository quirk registry loads the registered material quirk` 与 Python registry/profile
  门禁通过。
- exact-APK 已接受含 quirk Profile，并因本 WU 尚未映射运行时行为而保持相同 fail-closed
  边界。
- macOS-arm64 + ANGLE warnings-as-errors 构建及全量 CTest 405/405 通过。
