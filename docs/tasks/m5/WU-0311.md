# WU-0311 · second Profile activity Java declarations

目标：以纯 Profile 数据声明第二个 legacy APK 的 activity native init 实际缓存的 Java static method。

依赖：WU-0249、WU-0303、WU-0310、`src/session/MODULE.md`。

验收：

- [x] JADX 与目标 ELF 字符串/调用序列共同确认 activity native init 缓存的 15 项 static method 及精确签名。
- [x] 声明复用或命名通用 activity/process/license/analytics/online implementation id，生产代码不出现游戏分支。
- [x] C++ Profile 测试精确验证 class/name/signature/static/implementation 数据。
- [x] 真实 APK 越过 activity native init 的全部 `GetStaticMethodID`，下一未实现行为明确失败。
- [x] macOS/arm64 warnings-as-errors 构建、Profile validator 与全量 CTest 通过。
