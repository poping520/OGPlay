# WU-0312 · second Profile audio Java declarations

目标：以纯 Profile 数据声明第二个 legacy APK 的 audio native init 实际缓存的完整 Java static method 批次。

依赖：WU-0248、WU-0311、`src/session/MODULE.md`。

验收：

- [x] JADX 与目标 ELF 字符串/调用序列共同确认 GLMediaPlayer 缓存的 30 项 static method 及精确签名。
- [x] 与既有 SoundPool 行为相同的方法复用通用 audio implementation id；新增 looping/all-big/locale/browser/activity 语义使用通用命名，生产代码无游戏分支。
- [x] C++ Profile 测试精确验证 class、方法总数、关键签名与 static kind，Profile 保持在 200 行以内。
- [x] 真实 APK 越过 GLMediaPlayer native init 的全部 `GetStaticMethodID`，下一未实现行为明确失败。
- [x] macOS/arm64 warnings-as-errors 构建、Profile validator 与全量 CTest 通过。
