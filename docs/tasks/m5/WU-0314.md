# WU-0314 · deterministic legacy language index

目标：从统一确定性 Locale 配置实现 legacy Java phone-language 索引回调。

依赖：WU-0312、`src/runtime/framework/MODULE.md`、
`src/runtime/integration/MODULE.md`。

验收：

- [x] JADX 确认法/德/意/西/日/英/葡索引与未知语言回退契约。
- [x] 通用映射支持 ISO-639 两/三字母代码，复用 Locale 配置校验且不读取宿主区域设置。
- [x] JNI handler 返回受检配置对应的精确 `jint`，生产代码不含游戏、厂商或包名分支。
- [x] 第二个 exact APK 越过 `locale.detect_phone_language`；单帧 smoke 干净退出，120 帧
  smoke 推进至独立 A32 guest fault 边界。
- [x] macOS/arm64 warnings-as-errors 构建与全量 CTest 通过。
