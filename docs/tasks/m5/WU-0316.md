# WU-0316 · first-frame tick-budget diagnosis

目标：定位第二个 exact APK 在 VFS 修复后的新失败边界，并保留可继续验证的证据。

依赖：WU-0315、`src/runtime/execution/MODULE.md`。

验收：

- [x] 200,000,000 tick 的 120 帧有界 smoke 稳定复现单次 guest call 预算耗尽。
- [x] 临时诊断取得耗尽 PC `0x102eb2b0`，目标 ELF 反汇编定位为正常
  `InterpolateColours` 计算，不是 syscall 等待或自跳转循环。
- [x] 未完成效果验证的预算调整已撤销，生产默认值保持不变。
- [x] 当前限制与下一轮验证前提记录到 `KNOWN-ISSUES.md` 和滚动状态。
