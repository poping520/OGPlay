# WU-0320 · legacy big-audio batch pause/resume

目标：按 legacy Java 语义批量暂停和恢复现有 big audio voice。

依赖：WU-0319、`src/audio/MODULE.md`、`src/runtime/integration/MODULE.md`。

验收：

- [x] `pause_all_big` 只把 playing big voice 迁移为 paused，普通 pool 不变。
- [x] `resume_all_big` 只把 paused big voice 迁移为 playing，重复调用幂等。
- [x] 会话拥有的状态与真实 Ogg mixer 在同一 handler 下同步迁移。
- [x] Windows/MSVC warnings-as-errors、focused CTest 通过；full CTest 440/442，失败仅为
  独立既有 ETC1 参数与 VFS 大小写目录树用例。
- [x] exact APK 越过 `audio.resume_all_big`，推进至独立 ANGLE current-frame 边界。
