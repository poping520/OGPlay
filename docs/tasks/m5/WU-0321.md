# WU-0321 · guest finalization 保持 managed GL surface

目标：让 GLSurfaceView guest 的 module finalizer 在宿主关闭 managed ANGLE surface 前执行。

依赖：WU-0320、`src/session/MODULE.md`、`src/frontend/MODULE.md`。

验收：

- [x] lifecycle 明确编排 shutdown、guest finalization、surface close 的顺序。
- [x] frame 失败后的 Stop 仍按同一顺序完成 finalization 与 surface close。
- [x] Windows/MSVC warnings-as-errors 与 focused CTest 通过。
- [x] full CTest 440/442；失败仅为独立既有 ETC1 参数与 VFS 大小写目录树用例。
- [x] exact APK 跨过纹理创建后在 80 帧正常退出，不再发生无 current frame 的纹理清理。
