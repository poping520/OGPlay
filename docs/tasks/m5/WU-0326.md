# WU-0326 · legacy big-audio looping play

目标：让 legacy `(IFZ)V` big-audio play 在状态与真实 Ogg mixer 中保持 loop 语义。

依赖：WU-0325、`src/audio/MODULE.md`、`src/runtime/integration/MODULE.md`。

验收：

- [x] JADX 确认 boolean 参数进入 `MediaPlayer.setLooping` 后再 start。
- [x] `audio.play_sound_big_looping` 精确解码 resource、volume 与 JNI boolean。
- [x] looping voice 跨 PCM 尾部回绕并保持 active；普通 voice 仍自然结束。
- [x] Windows/MSVC warnings-as-errors 与 focused CTest 通过。
- [x] full CTest 441/443；失败仅为独立既有 ETC1 参数与 VFS 大小写目录树用例。
- [x] Release exact APK 越过原 missing handler，300 帧 32.513 秒、退出码 0。
