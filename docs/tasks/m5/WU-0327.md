# WU-0327 · Profile guest lifecycle suspend/resume

目标：让运行中的 GLSurfaceView guest 可按 Android Activity 切换语义暂停并恢复 native phase。

依赖：WU-0326、`src/session/MODULE.md`。

验收：

- [x] suspend 精确执行 pause phase，resume 精确执行 resume phase。
- [x] 挂起期间 frame 与 input 明确失败，重复/错序 suspend/resume 明确失败。
- [x] 挂起状态 Stop 不重复 pause，但仍执行 shutdown、finalization 与 surface close。
- [x] Windows/MSVC warnings-as-errors、focused CTest 通过；full CTest 441/443，失败仅为
  独立既有 ETC1 参数与 VFS 大小写目录树用例。

非目标：本 WU 不决定电影播放或跳过策略，也不按标题自动触发生命周期切换。
