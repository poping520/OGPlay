# WU-0338 · A32 tick exhaustion 现场

目标：A32 guest call 耗尽预算时输出 consumed tick、PC 与 LR，使昂贵路径可精确定位。

依赖：WU-0337、`src/runtime/execution/MODULE.md`。

验收：

- [x] budget exhaustion 同时报告 consumed tick、PC 与 LR，原有调用身份仍由上层保留。
- [x] exact Asphalt 5 定位到 PVRTC guest 软件解压而非死循环，不提高 Profile tick budget。
- [x] focused guest thread runner CTest 通过。

非目标：不改变切片大小、总 tick budget 或未处理 trap 语义。
