# WU-0343 · MCP click guest-loop 派发与 exact smoke

目标：在两个 `run-apk` guest loop 的主线程按相邻 step 派发 MCP click down/up，并通过
Asphalt 5 exact MCP 调用验证完整接线。

依赖：WU-0342、WU-0330、`src/frontend/MODULE.md`。

验收：

- [x] 可测试 dispatcher 每个 guest-loop step 最多消费一个 phase，并固定 pointer id 0。
- [x] MCP tap 等待已有桌面手势结束；tap active 时仅抑制桌面 pointer，不丢 key/quit。
- [x] NativeActivity 与 Profile GLSurfaceView loop 均只在主线程调用既有 input API。
- [x] 最终 Release Asphalt 5 exact MCP 接受 `(400,240)` 并返回 request 1/frame 428；帧从
  426 推进到 473。静态 Gameloft splash 不作为输入效果断言，不伪称画面已响应。
- [x] dispatcher focused CTest 2/2、Release 重建与 Windows full CTest 469/469 通过。

非目标：不实现 drag、多点、长按、键盘工具、动作完成等待或电影请求断言。
