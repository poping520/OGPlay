# WU-0344 · 固定官方 stb 图像编码器

目标：原样固定官方 `stb_image_write`，为 MCP 的跨平台 JPEG/压缩 PNG 输出提供受检依赖。

依赖：WU-0330、`src/agent/MODULE.md`。

验收：

- [x] `stb_image_write.h` 固定自官方 nothings/stb commit
  `2c980bb59875b0d32144a71867fbdebb2f77cd20`，SHA-256 为
  `cbd5f0ad7a9cf4468affb36354a1d2338034f2c12473cf1a8e32053cb6914a05`。
- [x] 上游头文件内的 MIT/Public Domain 双许可文本保持原样。
- [x] 实现单独编译且禁用 stdio；第三方告警不污染 OGPlay warnings-as-errors 契约。
- [x] Windows/MSVC Debug `ogplay_tests` 与 Release `ogplay` 均完成编译。

非目标：本 WU 不改变 MCP schema、默认格式或工具参数。
