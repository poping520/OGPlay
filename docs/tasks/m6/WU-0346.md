# WU-0346 · 固定端口 MCP 启动入口

目标：增加 `run-apk --mcp`，以固定端口 15971 启动现有 loopback MCP，同时保留自定义端口。

依赖：WU-0345、WU-0330、`src/frontend/MODULE.md`。

验收：

- [x] `--mcp` 无参数并精确映射 `http://127.0.0.1:15971/mcp`；默认仍不启动服务。
- [x] `--mcp-port <1..65535>` 语义不变；两入口互斥、各自拒绝重复并拒绝 `--preflight`。
- [x] usage 与 CMake 参数测试覆盖固定入口、双向冲突、重复和 preflight 失败。
- [x] Release Asphalt 5 exact `--mcp` smoke：固定 endpoint ping 成功，capture sequence 425
  返回 800×480、82,349 字节 JPEG，请求后进程保持运行。
- [x] Windows/MSVC full CTest 470/470 与 `git diff --check` 通过。

非目标：不自动修改任何 MCP 客户端配置，不改变协议、工具、鉴权或网络绑定范围。
