# WU-0347 · OGPlay MCP 测试使用文档

目标：用简练文档说明 OGPlay MCP 的启动、截图、点击、判定方法和当前自动化边界。

依赖：WU-0346、WU-0345、`docs/roadmap/10-ai-automation-testing.md`。

验收：

- [x] 文档同时说明固定 `--mcp`、自定义 `--mcp-port` 和默认关闭语义。
- [x] 测试流程覆盖首帧、JPEG/PNG 截图、guest 坐标点击、二次截图和正常清理。
- [x] 明确区分当前人工/AI 调试能力与尚未完成的结构化 CI 场景断言。
- [x] 全局文档索引、M6 任务索引与 AI 自动化 roadmap 均可到达使用文档，且文档反向链接规划。
- [x] documentation layout CTest 与 `git diff --check` 通过。

非目标：不包含任何 MCP 客户端配置，不新增运行时代码、协议工具或场景实现。
