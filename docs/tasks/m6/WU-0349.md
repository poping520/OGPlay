# WU-0349 · MCP 坐标网格使用文档

目标：用简练文档说明 AI 如何通过坐标网格定位点击，并用干净截图验证结果。

依赖：WU-0348、WU-0347。

验收：

- [x] 测试流程给出 JPEG/PNG 坐标网格调用和 guest 坐标点击示例。
- [x] 明确每 100 px 主线、每 25 px 边缘刻度以及顶部/左侧标签的定位语义。
- [x] 明确网格只作用于截图副本，验证证据应省略 `overlay` 获取干净截图。
- [x] documentation layout CTest 与 `git diff --check` 通过。

非目标：不包含 MCP 客户端配置，不修改运行时代码或工具 schema。
