# WU-0352 · MCP swipe 使用文档

目标：用简练文档说明 AI 如何结合坐标网格调用确定性 MCP swipe 并验证结果。

依赖：WU-0351、WU-0349。

验收：

- [x] 测试流程给出完整端点和步数的 swipe 调用示例。
- [x] 明确 `steps` 是 1..120 个 guest-loop motion 阶段，不是宿主毫秒。
- [x] 记录端点、步数、队列错误以及当前不支持的长按、多点触控和按键边界。
- [x] documentation layout CTest 与 `git diff --check` 通过。

非目标：不包含 MCP 客户端配置，不修改运行时代码或工具 schema。
