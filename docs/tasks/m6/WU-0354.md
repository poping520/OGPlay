# WU-0354 · macOS-arm64 MCP 严格构建修复

目标：修复近期 GPU JSON、MCP Base64 与 CPU result 测试在 macOS-arm64 Clang
warnings-as-errors 下的严格初始化/转换错误，恢复 M6 主线严格全量构建。

依赖：WU-0351、WU-0353。

验收：

- [x] GPU capability 的 `int64_t` limit 通过 JSON signed integer 接口保真序列化，正值输出
  契约保持不变。
- [x] MCP Base64 测试只在已验证字符区间后显式转换索引，不放宽告警或改变解码结果。
- [x] sliced CPU 测试显式初始化无 fault 的 `RunResult`，不改变模拟执行结果。
- [x] macOS-arm64 `cmake --build --preset dev`、相关 focused CTest 24/24 和全量 CTest
  478/478 通过。

非目标：不改变 MCP tool schema、场景契约、GPU provider 数据类型或运行时能力范围。
