# WU-0359 · Asphalt 5 exact 标题流场景与三轮 gate

目标：用 demo Asphalt 5 验证 Scenario→exact guest→输入/UI/lifecycle→证据→shutdown 闭环。

依赖：WU-0358、exact Asphalt 5 Profile、本地不入库 APK 与 API 19 ARMv7 Bionic fixture。

验收：

- [x] 场景只提交 exact Profile 身份和 `asphalt5.apk` logical fixture，不含宿主路径、APK 或
  游戏专属生产代码。
- [x] 选择 English 后有界推进到稳定标题帧，再点击 `(400,240)`；机器以 Main Menu 干净 PNG
  SHA-256 和无 guest fault 断言真实 UI 转移，不把 frame 推进伪作输入已响应。
- [x] 同一会话断言 suspend/resume lifecycle，所有 checkpoint 产出声明的相对 frame/state/
  log 证据并正常 shutdown、进程退出 0。
- [x] macOS-arm64 同一场景连续三轮全部 passed；关键 checkpoint 三轮均为 frame/ticks
  468/468000、PNG SHA-256 `9ee57323dae576c38d4d29984c067b5bceaa86f77724c8f3b174bcd1a81962b8`、
  shutdown requested/clean true，证据只在忽略的 `.local/automation/`。
- [x] Scenario focused tests 5/5、warnings-as-errors build、full CTest 484/484、
  capabilities/CURRENT 与 `git diff --check` 通过。

实测更正：旧状态曾推测标题页触摸会重复发布 logo movie request；本场景的原子状态和截图
证明该 exact 路径直接进入 Main Menu 且 movieRequest 为 null，因此以真实 UI golden 断言，
不伪造 movie 成功。

非目标：不提交 demo APK/API 19 文件或运行证据，不实现 movie playback，也不增加标题分支。
