# WU-M8-010 · integration 契约收敛与模块文档瘦身

一句话目标:按 ADR-0018 重写 `runtime/integration`、`runtime/jni_guest`、
`runtime/boundary` 三份 MODULE.md 与模块索引,使契约与拆分后的代码事实一致。

依赖:WU-M8-008、WU-M8-009。

## 范围

- `src/runtime/integration/MODULE.md` 重写:职责收敛为"装配 + 会话拥有的 Java handler
  状态 + runner/contract",移除已迁出的 jni_guest/boundary 不变量,不变量总量只减不增。
- `src/runtime/jni_guest/MODULE.md`、`src/runtime/boundary/MODULE.md` 定稿:依赖定位、
  迁入不变量与测试引用完整。
- `docs/modules/INDEX.md` 增补两个子模块行;`docs/tasks/m8/README.md` 登记
  WU-M8-008..010;`docs/state/CURRENT.md` 滚动快照更新。
- 核对 `capabilities.toml`:能力项与测试引用无路径失效(能力状态不变)。

非目标:不改代码行为;不做 CMake target 拆分(留待后续按 ADR-0018 后果推进)。

## 验收(机器可判定)

- [x] `ctest --preset dev` 全量通过(macOS/arm64 524/524,含 `documentation layout`
  门禁,新子模块 MODULE.md 存在性受检)。
- [x] 三份 MODULE.md 行数合计(94+86+147=327)少于拆分前 integration 单份契约
  (328 行);重复的"明确失败/先预检后提交"从句收敛为 boundary 总则。
- [x] `docs/modules/INDEX.md` 的 runtime 子模块表包含 jni_guest 与 boundary。
