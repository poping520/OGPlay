# WU-M9-025 · Profile v1 完全移除

## 目标（一句话）

迁移剩余生产 Profile 到 v2，并删除 v1 schema、解析、Java 映射与 native-call 重放链。

## 验收

- 仓库生产代码与 `data/profiles/` 不再接受或生成 schema 1。
- 删除 v1 专属源文件、公共头、CTest 与 capabilities 条目，不保留死分支。
- Asphalt 5 exact gate 保持逐位一致；Dungeon Hunter/A6 均由 v2 Profile 精确匹配。

## 结果（已完成）

- 删除 v1 JSON schema、解析模型、Java registry、native target/invocation/execution、旧
  lifecycle/plan/bootstrap 源文件、公共头和专属 CTest；生产目录仅保留三个 v2 Profile。
- capabilities 的历史 ID 因单调账本规则保留，但均改为“已退役”说明并指向 v2 拒绝测试，
  不再引用已删除文件或能力实现。
- Asphalt 5 exact 回归 passed：468 frame/468000 tick、主界面 SHA-256
  `9ee57323dae576c38d4d29984c067b5bceaa86f77724c8f3b174bcd1a81962b8`、无 fault、
  suspend/resume 与 clean shutdown 全通过；Dungeon Hunter/A6 production Profile 均迁至 v2。
