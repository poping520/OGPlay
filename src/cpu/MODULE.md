# 模块：cpu

## 职责

执行 guest A32/T32/A64 指令，管理每线程寄存器与异常；提供 JIT 和解释器双后端。

## 公共 API

M1 定义 `Cpu::Run`、寄存器访问和 fault/陷阱结果。

## 不变量

- 每个 guest 线程拥有独立执行上下文。
- 内存失败产生 Fault，不得返回零；CPU 不直接调用 HLE。

## 禁止

- 不得包含游戏分支或直接宿主 IO。
- 不得绕过 memory 接口长期保存宿主裸指针。

## 测试

`tests/cpu/` 的解释器/JIT 指令级对拍。

