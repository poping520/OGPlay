{
  "name": "ogplay-webui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "check": "tsc --noEmit && vitest run",
    "build": "npm run check && vite build && node scripts/manifest.mjs"
  },
  "dependencies": { "preact": "10.29.8" },
  "devDependencies": { "vite": "8.3.0", "typescript": "5.9.3", "vitest": "4.1.0" }
}
